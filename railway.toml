[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
deployCommand = "npx drizzle-kit push --force"
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
